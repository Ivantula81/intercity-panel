# План внедрения продаж и возвратов

Дата: 2026-09-02
Статус: выполнено на production; первичная 30-дневная догрузка продолжается по cron

## Цель и критерий успеха

Новые письма о продаже или возврате автоматически и без ручного `sales_seed.php`
попадают во вкладку «Продажи». Повторная обработка письма не создаёт дубль, а
экран показывает время и результат последней синхронизации.

## План — 8 шагов

1. [x] Настроить read-only доступ к почтовому ящику; секреты хранить только в `/etc/panel.env`.
2. [x] Добавить миграцию состояния синхронизации, бизнес-идентификаторов и ошибок импорта.
3. [x] Реализовать CLI-загрузчик писем с курсором: читать только новые сообщения и не менять их в ящике.
4. [x] Доработать `SalesParser`: продажи, возвраты, отмены, несколько билетов в одном письме и дата относительно даты письма.
5. [x] Сделать идемпотентную запись: дедупликация по ID письма и по идентификатору заказа/билета.
6. [x] Сохранять нераспознанные письма в журнал ошибок, не добавляя их в показатели продаж.
7. [x] Показать во вкладке события, время последней синхронизации, состояние источника и предупреждение об устаревших данных.
8. [x] Добавить фикстуры и тесты, выполнить dry-run, затем включить загрузчик по cron и проверить контрольные продажу и возврат в панели.

## Ограничения и риски

- Затронутые процессы: импорт продаж, возвратов и отображение аналитики.
- Затронутые риски: `RISK-011`, смежно `RISK-010` и `RISK-012`.
- Нельзя допустить: дубли, потерю возврата, ложный статус свежих данных, двойной учёт продажи и оплаты.
- Обязательные негативные проверки: повторный запуск, несколько билетов в письме,
  возврат до продажи, неизвестный отправитель, испорченное письмо, обрыв соединения.
- Во время тестов запрещены удаление/изменение писем и запись тестовых событий в
  управленческие показатели продакшена.

## Подключение Gmail

На сервере требуется `php8.3-imap`, применение `schema25.sql` и переменные:

```text
SALES_IMAP_HOST=imap.gmail.com
SALES_IMAP_PORT=993
SALES_IMAP_SECURITY=ssl
SALES_IMAP_MAILBOX=INBOX
SALES_IMAP_USERNAME=ivanmach.notifications@gmail.com
SALES_IMAP_PASSWORD=<пароль приложения Gmail>
SALES_IMAP_LOOKBACK_DAYS=30
SALES_IMAP_BATCH_SIZE=200
```

Сначала запускается `php scripts/sales_ingest.php --dry-run`; cron включается
только после сверки контрольной продажи и возврата.

## Фактический результат 2026-09-02

- Код выпущен через PR №20–22; production работает на `main`.
- Перед `schema25` создан и проверен защищённый полный дамп БД.
- Gmail подключён read-only, PHP IMAP установлен, секрет хранится только в `/etc/panel.env`.
- Cron `/etc/cron.d/panel-sales-ingest` запускается каждые 5 минут под `www-data`
  с `flock`; журнал ограничен logrotate.
- Первый автоматический пакет: 195 импортировано, 5 пропущено, 0 ошибок;
  дубликатов `event_key` — 0.
- В production-вкладке за 30 дней подтверждены продажи, возвраты, аннуляции,
  разбивка по каналам и лента входящих.

## Этап 2 — агенты и принадлежность продаж

Критерий успеха: каждая новая продажа/возврат сохраняет исходные адреса письма,
в панели получает агента и сторону «наши / перевозчик», а неизвестные данные
явно остаются нераспределёнными.

1. [x] Сохранить исходные `From`, `To` и заголовок, из которого взят получатель.
2. [x] Добавить перевозчикам уникальные адреса уведомлений и отдельный список наших адресов.
3. [x] Определять сторону только по точному совпадению исходного получателя.
4. [x] Добавить правила агентов: тег, отправитель, необязательная часть темы и связь с агентом отчётности.
5. [x] Не угадывать при неизвестном или конфликтующем адресе; показывать «Не распределено».
6. [x] Добавить фильтры по агенту, стороне, перевозчику, отправителю, получателю и теме.
7. [x] Сделать повторную классификацию загруженных строк и безопасный IMAP-backfill без движения рабочего курсора.
8. [x] Добавить проверки нормализации, приоритетов, конфликтов адресов и полный preflight.

Для исторических строк после backup и применения `schema26.sql` используется:

```text
php scripts/sales_ingest.php --backfill-classification
```

Backfill имеет отдельный курсор `gmail_sales_headers_backfill`, не останавливает
обычный импорт и обновляет только уже существующие продажи. Письма вне рабочего
окна он не добавляет в управленческие показатели.
