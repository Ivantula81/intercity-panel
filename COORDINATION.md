# Координация агентов (Claude + Codex)

Над панелью работают два ИИ-агента. Эти правила — чтобы не толкаться в одних файлах и видеть работу друг друга через GitHub.

## Где кто работает
| | Каталог (worktree) | Ветка |
|---|---|---|
| **Claude** | `/root/panel-claude` | `claude/<задача>` |
| **Codex** | `/root/panel-codex` | `codex/<задача>` |
| деплой/интеграция | `/root/panel-src` | `main` — тут НЕ работают руками |

Worktrees делят одну git-историю, но у каждого свой каталог и ветка — файлы друг друга не перезаписываются.

## Правила
1. **Перед началом:** `git fetch --all && git log --all --oneline` — увидеть, что сделал другой.
2. Коммить часто; каждый коммит **авто-пушится** на GitHub (хук `post-commit`).
3. Правишь только файлы **своей зоны** (см. ниже). Общие файлы — согласовать в журнале.
4. Перед коммитом — **lint**: `php -l <файл>`, `node --check public/assets/panel.js`.
5. Готовый и проверенный кусок → **PR** (`gh pr create`) → merge в `main` → деплой `/root/deploy.sh`.
6. **Живой сайт — только из `main`.** WIP-ветки на прод не льём.
7. Меняешь дизайн/логику — **сначала макет/план, потом код** (требование владельца).

## Зоны ответственности
| Claude | Codex |
|---|---|
| Чаты / каналы / inbox / фронт | Надёжность рассылок + модель данных + рефакторинг |
| `app/views/chats.php`, чат-JS в `panel.js`, `public/*-webhook.php`, `lib/Channels.php`, UI-вьюхи, каналы/Telegram | outbox + идемпотентность (#27), conversations-модель, разбор `app/api.php` (#28), тесты (#29), безопасность |

⚠️ **Общий файл — `app/api.php`.** Пока Codex его разбивает (#28) — Claude туда не лезет. Договариваемся в журнале ниже.

## Деплой (как выкатить свою работу)
```
gh pr create            # из своей ветки
# ...мёрдж PR в main на GitHub...
git -C /root/panel-src pull && /root/deploy.sh
```

## Журнал работ (новое — сверху)
- **2026-06-22 · Codex:** ветка `codex/chat-inbox-foundation-review` — ревью этапа 1a Claude и продолжение согласованного inbox-плана: conversations/events, backfill+dual-write, очереди, статусы, assignee, priority, notes, cursor pagination и точное разделение каналов. Детали: `docs/chat-review-2026-06-22.md`.
- **2026-06-21 · Claude:** завершён Этап 0 UX-фиксов (мобайл, ошибки API, канал в чатах, период дашборда, цвета каналов). В работе — Этап 1 «Чаты как inbox»: макет согласован, жду старт.
- **2026-06-21 · Codex:** проведён UX-аудит (`docs/ux-audit/`, `CODEX_RECOMMENDATIONS.md`). Рекомендуемая зона дальше — надёжность рассылок (outbox/идемпотентность) и conversations-модель.
